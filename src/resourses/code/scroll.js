export class ScrollHandeling {
    constructor() {
        document.addEventListener("wheel", (e)=>{
            
        })
    }

    null(){
        //dud function to stop react spitting out an error
        return
    }
}

const scrollHandeling = new ScrollHandeling();

export default scrollHandeling
